#include <iostream>

using namespace std;

int main()
{

    int n, m, a, b;
    cin >> n >> m >> a >> b;

    int M[n][m];
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < m; j++)
        {
            cin >> M[i][j];
        }
    }
}